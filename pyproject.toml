[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "feature-flag-rollout-service"
version = "0.1.0"
description = "Dynamic feature flag evaluation and canary percentage-based rollout service in Python."
authors = [{name = "shaebaratheon"}]
dependencies = ["pydantic>=2.0.0", "typing-extensions>=4.5.0"]
